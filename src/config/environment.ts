export const DEV = NODE_ENV != 'production';
export const LOG_RETENTION_DAYS = 30;
export const APP_VERSION = PUBLIC_APP_VERSION;
export const BUILD_DATE = PUBLIC_BUILD_DATE;
export const BUILD_TAG = PUBLIC_BUILD_TAG;
